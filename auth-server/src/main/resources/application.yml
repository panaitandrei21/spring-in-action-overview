spring:
  application:
    name: auth-server
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
server:
  port: 9000


logging.level.org.springframework.security: DEBUG
logging.level.com.authserver: INFO